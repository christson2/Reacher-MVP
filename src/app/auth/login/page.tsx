import LoginPageComponent from '@/modules/auth/pages/login';

export const metadata = {
  title: 'Sign In - Reacher',
  description: 'Sign in to your Reacher account',
};

export default function LoginPage() {
  return <LoginPageComponent />;
}
